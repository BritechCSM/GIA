{
    "name": "gia-root",
    "version": "1.0.0",
    "private": true,
    "ワークスペース": [
        "web"
    ],
    "scripts": {
        "dev": "npm --prefix web run dev",
        "build": "npm --prefix web run build",
        "start": "npm --prefix web run start",
        "lint": "npm --prefix web run lint",
        "install-web": "npm install --prefix web --legacy-peer-deps"
    },
    "dependencies": {
        "next": "latest"
    }
}